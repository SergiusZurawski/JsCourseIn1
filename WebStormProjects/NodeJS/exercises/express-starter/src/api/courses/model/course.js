export class Course {
    constructor(code, title, difficulty, duration) {
        this.code = code;
        this.title = title;
        this.difficulty = difficulty;
        this.duration = duration;
    }
}